
s1, s2 = set(input("Enter the first list (comma separated): ").split(",")), set(input("Enter the second list (comma separated): ").split(","))
# & symbol finds elements present in both sets
print(s1 & s2)